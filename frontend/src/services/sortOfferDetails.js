// export default function sortOfferDetails(offerDetails) {
//   //   Object.assign(
//   //     [],
//   offerDetails.sort((a, b) => {
//     if (a.price < b.price) return -1;
//     if (a.price > b.price) return 1;
//     return 0;
//   });
//   //   );
// }
